#include <gtest/gtest.h>

#include "solve.hpp"

using namespace wheels;

TEST(tensor, armadillo) {
  //using namespace arma;
  //mat A = randu<mat>(5, 5);
  //vec b = randu<vec>(5);
  //mat B = randu<mat>(5, 5);

  //vec x1 = solve(A, b);

  //vec x2;
  //bool status = solve(x2, A, b);

  //mat X1 = solve(A, B);

  //mat X3 = solve(A, trimatu(B)); // indicate that B is triangular
}